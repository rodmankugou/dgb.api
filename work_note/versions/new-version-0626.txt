1、上线前

ALTER TABLE `auction_record` ADD COLUMN `need_cancel_flag` tinyint(1) COMMENT '流拍单是否需要平台调用合约的cacnel接口。0-否 1-是' AFTER `auto_confirm_flag`;
CREATE TABLE `cancel_task` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `auction_record_id` bigint DEFAULT NULL,
  `status` tinyint DEFAULT NULL COMMENT '0-未开始 1-成功 2-失败',
  `tx_hash` varchar(255) DEFAULT NULL COMMENT '交易hash',
  `process_time` bigint DEFAULT NULL,
  `create_time` bigint DEFAULT NULL COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `tx_hash` (`tx_hash`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8

上线后
update auction_record set need_cancel_flag = 0 where create_time < 1656148800000;
update auction_record set need_cancel_flag = 1 where need_cancel_flag is null;